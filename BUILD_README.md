This project is managed using `git submodule` and gradle `composite build`.

- `git submodule` is used to include project `x12q_common` from a different repository
- gradle `composite build` is for including `x12q_common` into other modules.
